# Состояние работы

Обновляется в конце каждой сессии. Начало следующей сессии — «продолжай, сессия N».

## Разбивка на сессии

| # | Сессия | Статус |
|---|---|---|
| 1 | Каркас + слой данных + карта с посёлками | **готово** |
| 2 | Линии заявок, модалка, счётчик пула, кнопка по порогу | **готово** |
| 3 | Построение рейса, маршрут на карте, панель экономики, OrdersPage, ReportPage | **готово** |
| 4 | `/demo` для телефона, Supabase Realtime, стыковка с реальным бэкендом, прогон ×3 | не начата |

---

## Сессия 1 — готово

### Сделано

- Проект перенесён из корня в `frontend/` через `git mv`. В корне: `README.md`, `CLAUDE.md`, `context/`.
- Ветка `feat/frontend` от `main` (ветки `base` в репозитории нет).
- Зависимости: TanStack Query + devtools, react-router-dom, maplibre-gl, react-markdown, @supabase/supabase-js.
- shadcn-компоненты: `dialog`, `table`, `badge`, `progress`, `card` (+ уже был `button`).
- `src/config/constants.ts` — константы по плану, плюс `MAP_LABEL_FONT` и `ORDER_LINE_COLORS`.
- `src/api/types.ts` — контракт API дословно по `plan-frontend.md` §4.
- `src/api/mock-data.ts` — 12 посёлков с координатами из Nominatim + 6 заявок на 520 кг + генератор мок-рейса.
- `src/api/client.ts` — обёртка над fetch; при `USE_MOCK_API` обслуживает запросы локальным хранилищем
  (включая `demo/seed` с исчерпанием набора и `demo/reset`).
- `src/api/queries.ts` — `queryKeys` + `useSettlements`/`useOrders`/`usePool`/`useTrip` + мутации.
- `src/map/` — `MapView` (инстанс в ref, контекст), `MapContext`, `useSettlementMarkers` (один source, два слоя, `fitBounds`).
- `src/routes/` — `MapPage` живая, `OrdersPage`/`ReportPage`/`DemoPage` заглушки.
- `App.tsx` — шапка с табами; `/demo` вынесен отдельным полноэкранным роутом без шапки.
- `index.html` — `lang="ru"`, осмысленный `<title>`.
- `vercel.json` (SPA-rewrites), `.env.example`.

### Проверено

- `npm run build` — проходит.
- Дев и прод-сборка: карта Мангистау, 12 подписанных посёлков, `fitBounds` по всей области, Актау выделен как хаб.
- Консоль без ошибок.
- `/demo` рендерится без шапки.

### Принятые решения

- **Справочник — 12 посёлков.** «Устюрт» из ТЗ выброшен: это плато, а не населённый пункт,
  Nominatim отдаёт на него школу в Бейнеу. Заменён на **Сайөтес** — реальное село Мангистауского
  района, упоминается в Р-01 ТЗ. Требование «минимум 10» выполнено с запасом.
- **Демо-набор даёт ровно 520 кг** (80+120+60+95+85+80), первые пять = 440 < 500.
  Это цифра, которую спикер называет вслух на 25-й секунде демо.
  В `plan-backend-1.md` черновая таблица даёт 605 — **нужно согласовать с бэкендом-1**.
- **maplibre-gl запинен на `^5`** — см. CLAUDE.md, в 6.4.1 не создаётся воркер.
- **Тестов нет** осознанно: верификация через `npm run build` и браузер.

### Что осталось за мной, но требует тебя

- **Деплой на Vercel.** Root Directory = `frontend/`, фреймворк Vite, переменных не нужно
  (мок включается сам). Не могу залогиниться в твой аккаунт — сделай через GitHub-интеграцию
  или дай залогиненный `vercel` CLI.
- **Передать бэкенду-1** согласованный демо-набор на 520 кг.

---

## Сессия 2 — готово

### Сделано

- `src/map/useOrderLines.ts` — один source `order-lines`, цвет линии из `['get','color']`,
  обновление через `setData`. Слоя два: видимый (3 px) и прозрачный «мишень» шириной 18 px —
  в трёхпиксельную линию на проекторе не попасть. Выбранная линия утолщается до 6 px
  через `setPaintProperty`. Линии вставляются **под** кружки посёлков (`beforeId`).
- `src/features/orders/OrderModal.tsx` — модалка заявки: код, цветная точка, статус, отправитель,
  маршрут, груз, вес, места, габариты. После построения рейса добавляется блок «В сводном рейсе»
  с очередью выгрузки, местом погрузки, плечом и стоимостью из `Trip.orders`.
- `src/features/pool/PoolCounter.tsx` — `520 / 500 кг · 6 заявок`, прогресс-бар,
  кнопка «Построить рейс» только при `pool.isReady`. Переход через порог красит панель,
  цифру, полосу прогресса в зелёный, кнопка въезжает снизу (`animate-in slide-in-from-bottom-2`).
- `src/lib/format.ts` — `formatNumber/formatKg/formatKzt/formatKm` на `Intl` с русской локалью
  и `pluralRu` для «6 заявок».
- `MapPage.tsx` — состояние `selectedOrderCode`, колонка оверлеев слева
  (`pointer-events-none` на колонке, `auto` на панелях — драг карты не перехватывается).

### Проверено (Playwright, дев-сервер)

- Шесть разноцветных линий Актау → посёлок, все шесть пунктов назначения различимы.
- Клик по розовой линии в Бейнеу открывает модалку `ORD-006` с правильными данными.
- Кнопка «Построить рейс» строит мок-рейс: статус заявки становится «В рейсе»,
  в модалке появляется блок «В сводном рейсе» (6-я остановка, 481 км, 103 501 ₸).
- Пул на пяти заявках: `440 / 500 кг`, панель нейтральная, кнопки нет.
  На шести: `520 / 500 кг`, панель зелёная, кнопка есть. Проверено временной правкой мока.
- Консоль без ошибок и предупреждений. `npm run build` и `npm run lint` проходят
  (в lint только два унаследованных `react-refresh` от shadcn-компонентов).

### Принятые решения

- **Кнопка «Построить рейс» уже вызывает `POST /api/trips`.** Мёртвая кнопка на демо выглядит
  хуже недоделанной фичи, а мутация `useCreateTrip` была готова с сессии 1. Видимый эффект
  нажатия — данные рейса в модалке; **линия маршрута на карте и панель экономики — сессия 3**.
  Пока рейс построен, вместо кнопки в панели пула стоит плашка «Рейс TRIP-001 построен» —
  в сессии 3 её место займёт `TripSummary`.
- **Прозрачный слой-мишень вместо расширения самой линии.** `line-opacity: 0` не мешает
  `queryRenderedFeatures` находить фичу, так что клик ловится на 18 px, а рисуется 3 px.
- **Выбранная заявка ищется в кеше по коду, а не хранится копией в state** — после построения
  рейса у неё меняется статус, и копия показывала бы «Новая» вместо «В рейсе».

### Известные мелочи, не блокеры

- Подписи посёлков дублируются с подписями стиля OpenFreeMap (видно на Бейнеу, Шетпе, Курыке):
  наш `symbol`-слой рисует свою подпись поверх чужой. Лечится фильтром на слое стиля —
  сделать в полировке сессии 4, на демо не критично.

---

## Сессия 3 — готово

### Сделано

- `src/features/trip/` — общее состояние рейса. `CurrentTripProvider` держит id построенного
  рейса над роутами и дублирует его в `localStorage`; `useCurrentTrip` отдаёт сам рейс через
  `useTrip`. Мутация `createTrip` больше не источник правды: она живёт только на карте,
  а рейс нужен ещё в таблице и в отчёте. Если рейса на бэкенде нет (404 — демо сбросили),
  id вычищается, и страницы показывают пустое состояние, а не ошибку.
- `src/map/useRouteLine.ts` — `Trip.routeGeometry` двумя слоями (белая обводка + тёмная линия)
  между линиями заявок и кружками посёлков, плюс пронумерованные остановки отдельным
  источником: номера берутся из `stopOrder` без первого и последнего элемента — это хаб.
  Когда рейс исчезает, слои и источники снимаются.
- `useOrderLines` получил `isDimmed`: после построения рейса линии заявок уходят на
  `ORDER_LINE_DIMMED_OPACITY`, но выбранная остаётся яркой — иначе непонятно, про какую
  заявку открыта модалка.
- `src/features/trip/TripSummary.tsx` — «было / стало / сэкономлено» с процентом и ссылкой
  «Показать отчёт». Заменил плашку «Рейс построен» в `PoolCounter` (у него теперь `hasTrip`).
- `src/features/orders/OrdersTable.tsx` + `OrdersPage` — таблица с цветным маркером, кодом,
  маршрутом, грузом, весом, местами и статусом; после рейса добавляются колонки очереди
  выгрузки и стоимости, а строки сортируются по `dropIndex` — таблица читается как
  маршрутный лист. Итоговая строка: суммарный вес и сумма стоимостей. Клик по строке
  открывает ту же `OrderModal`, что и клик по линии на карте.
- `src/features/report/ReportView.tsx` + `ReportPage` — Markdown через `react-markdown`
  с поэлементной типографикой (плагина `@tailwindcss/typography` в проекте нет).
  Шапка документа: код рейса, бейдж источника отчёта, пробег и экономия.
- `buildMockReport` в `mock-data.ts` — отчёт мока стал настоящим документом
  (маршрут, порядок погрузки, таблица выгрузки, экономика) вместо строки-заглушки.

### Проверено (Playwright, дев-сервер)

- Нажатие «Построить рейс» рисует ломаную Актау → Акшукур → Курык → Жетыбай → Шетпе →
  Жанаозен → Бейнеу → Актау с кружками 1…6, линии заявок приглушены.
- Панель экономики: было 1 992 км, стало 1 280 км, экономия 712 км (−36%) и 127 092 ₸.
- Клик по приглушённой линии открывает `ORD-005`, сама линия остаётся яркой и толстой.
- «Заявки»: шесть строк в порядке выгрузки, итог 520 кг и 228 480 ₸ = сумма стоимостей.
- «Отчёт»: таблица, списки и заголовки отрендерены; цифры совпадают с картой и таблицей.
- Перезагрузка `/report` при живом id, но мёртвом моке (хранилище мока в памяти) даёт
  пустое состояние без ошибки — сработала чистка id по 404.
- `npm run build` проходит. `npm run lint` — те же два унаследованных `react-refresh`
  от shadcn-компонентов, своих замечаний нет. Консоль браузера чистая.

### Принятые решения

- **id рейса в `localStorage`, а не список рейсов с бэкенда.** В контракте нет ручки
  «дай последний рейс», а F5 посреди демо не должен стирать маршрут. Менять контракт
  ради этого не стали.
- **Добавлен `remark-gfm`.** Отчёт пишет Gemini, таблицы в нём почти неизбежны,
  а без плагина GFM-таблица отрендерилась бы простым текстом.
- **Отчёт мока пишется по данным рейса.** Если модель на бэкенде не ответит, вкладка
  «Отчёт» всё равно покажет документ, а не пустоту.
- **Маршрут с белой обводкой.** Тёмная ломаная теряется на тёмных участках стиля Liberty.

### Известные мелочи, не блокеры

- В моке обратное плечо Бейнеу → Актау совпадает с прямой линией заявки `ORD-006`
  и перекрывает её. На реальной геометрии от ORS ломаная пойдёт по дорогам, и совпадения
  не будет — проверить после стыковки с бэкендом.
- Снятие слоёв маршрута при сбросе рейса написано, но вживую не проверялось: кнопки
  «Сброс» ещё нет, она появится вместе с `/demo` в сессии 4.
- Подписи посёлков всё ещё дублируются с подписями стиля OpenFreeMap (из сессии 2).

---

## Сессия 4 — план

Из `plan-frontend.md` H+5:15 — H+6:00 и H+8:00 — H+10:00:

- `/demo` — две крупные кнопки на весь экран: «Создать заявки» (`POST /api/demo/seed`
  ×`DEMO_ORDERS_COUNT` с интервалом `DEMO_ORDER_INTERVAL_MS`) и «Сброс», плюс счётчик
  отправленных заявок.
- `src/realtime/useOrdersRealtime.ts` — подписка Supabase на INSERT в `orders`,
  событие используется только как сигнал инвалидации. Запасной вариант — `refetchInterval`.
- Стыковка с реальным бэкендом: `VITE_API_BASE_URL`, прогон всего флоу, правка расхождений.
- Полировка: фильтр на дублирующиеся подписи посёлков, проверка снятия слоёв при сбросе.
- Прогон демо трижды подряд через «Сброс».

Готово, когда: с телефона нажимается кнопка, на ноутбуке появляются заявки,
и весь сценарий повторяется трижды без перезагрузки страницы.
