/** Заглушка. Markdown-отчёт — сессия 3. */
export function ReportPage() {
  return (
    <div className="p-6">
      <h1 className="text-xl font-medium">Отчёт</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Отчёт по рейсу появится здесь после построения.
      </p>
    </div>
  );
}
